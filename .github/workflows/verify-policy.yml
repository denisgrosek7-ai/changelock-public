name: verify-policy
on:
  pull_request:
    paths:
      - 'policies/**'
      - 'deploy/kyverno/**'
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: echo "validate policy schema here"
