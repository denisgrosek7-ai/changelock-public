apiVersion: v1
kind: Pod
metadata:
  name: changelock-deny-latest
  namespace: acme-prod
  annotations:
    changelock.io/tenant: acme
    changelock.io/repository: my-org/acme-app
    changelock.io/subject: repo:my-org/acme-app
    changelock.io/workflow-ref: refs/heads/main
    changelock.io/workflow-sha: abc123
spec:
  serviceAccountName: changelock-demo
  securityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: app
      image: ghcr.io/my-org/acme-app:latest
      securityContext:
        runAsNonRoot: true
        readOnlyRootFilesystem: true
        allowPrivilegeEscalation: false
        capabilities:
          drop: ["ALL"]
